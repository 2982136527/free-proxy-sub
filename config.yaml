server:
  host: "0.0.0.0"
  port: 8000

github:
  token: ""
  search_queries:
    - "free proxy subscription"
    - "shadowsocks config"
    - "v2ray config"
    - "clash config"
    - "ss://"
    - "vmess://"
    - "trojan://"
    - "proxy list"
    - "机场 订阅"

crawler:
  interval_minutes: 360

validator:
  interval_minutes: 30
  concurrency: 50
  connect_timeout: 5
  max_dead_count: 3

subscription:
  max_proxies: 200
  selected_count: 30
  name: "FreeProxyHub"
